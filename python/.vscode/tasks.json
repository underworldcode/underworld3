{
    // See https://go.microsoft.com/fwlink/?LinkId=733558 
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "build swarm example",
            "command": "/usr/bin/make",
            "args": [
                "compile"
            ],
            "options": {
                "cwd": "${fileDirname}"
            },
            "group": "build"
        }
    ]
}